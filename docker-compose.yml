version: "3.8"
services:
  cloudlymccloudflare:
    build: .
    ports:
      - "8787:8787"
    environment:
      - CF_API_TOKEN=${CF_API_TOKEN}
      - CF_ACCOUNT_ID=${CF_ACCOUNT_ID}
      - APP_SECRET=${APP_SECRET}
    volumes:
      - ./data:/app/data
